package t3n:enclave;

interface contracts {
    execute: func(input: string) -> result<string, string>;
}

world enclave {
    // Confidential host capabilities (see wit/host.wit).
    import kv;
    import http;

    export contracts;
}
